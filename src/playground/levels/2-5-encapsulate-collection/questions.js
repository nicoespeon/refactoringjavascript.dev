// You can't remove `export` on the array, even when encapsulated
// because the playground is resetting the references behind the scenes.
// But in a real codebase, you would be able to do so!

export const popQuestions = new Array();

export const scienceQuestions = new Array();

export const sportsQuestions = new Array();

export const rockQuestions = new Array();
